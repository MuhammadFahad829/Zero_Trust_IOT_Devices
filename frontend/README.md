# Frontend

React dashboard for the ZeroTrust IoT Gateway.

## Development

```bash
cd frontend
npm install
npm start
```

## Production Build

```bash
cd frontend
npm run build
```

## Notes

- Tailwind CSS, PostCSS, and Autoprefixer are already configured.
- If dependency warnings appear, run `npm install` again.
